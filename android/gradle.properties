# JVM 内存配置
# 为了解决 Android Lint 在 CI 中发生的 Metaspace OOM，将 metaspace 提升到 2g，并开启堆转储以便诊断
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=2g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# 禁用守护进程以避免跨构建的内存积累
org.gradle.daemon=false
